@{
    # Canonical casing for commands that are not declared as functions in this repository.
    # Add a command here when a new literal invocation is intentionally introduced.
    Commands = @(
        'ForEach-Object'
        'Get-ChildItem'
        'Get-Content'
        'Get-Item'
        'Get-Verb'
        'Import-Module'
        'Import-PowerShellDataFile'
        'Invoke-Pester'
        'Invoke-ScriptAnalyzer'
        'Join-Path'
        'New-Item'
        'Remove-Item'
        'Select-Object'
        'Set-Content'
        'Should'
        'Sort-Object'
        'Split-Path'
        'Start-Sleep'
        'Where-Object'
        'Write-Host'
        'Write-Output'
        'gh'
        'git'
        'pwsh'
    )
}
